#include <stdio.h>

int main() {
    int i, j, m[3][3]={0}, soma=0;
    for(i=0; i<3; i++){
        for(j=0; j<3; j++){
            scanf("%i", &m[i][j]);
           if(i==(j+1)){
               soma+=m[i][j];
           }
            }
        }
    printf("\nSOMA=>%i", soma);
    return 0;
}
